import React from 'react'

const Btn_React = () => {
  return (
    <div>
        {/* If we import react we can directly give the button value no need to call and give props in parent App.jsx */}
      <button>Home</button>
      <button>About</button>
      <button>Services</button>
    </div>
  )
}

export default Btn_React
